import './section.scss'

import Sort from '../sort'
import Tickets from '../tickets'

function Section() {
  return (
    <section className="section">
      <Sort />
      <Tickets />
    </section>
  )
}

export default Section
